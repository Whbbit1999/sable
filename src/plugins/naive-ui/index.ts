import { App } from 'vue'

export function setupNaiveUI(app: App) {}

const lightThemeOverrides = {
  common: {
    primaryColor: '#ECC666',
  },
}
const darkThemeOverrides = {
  common: {
    primaryColor: '#ECC666',
  },
}
